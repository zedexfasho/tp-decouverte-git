name: CI

on:
  push:
    branches: [ "main", "feature-junior" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: echo "CI OK"

